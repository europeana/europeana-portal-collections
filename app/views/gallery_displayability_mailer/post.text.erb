Automated gallery displayability report for Europeana gallery: <%= @gallery.title %>

URL: <%= gallery_url(:en, @gallery) %>

Image error(s) follow.

<% @image_errors.each_pair do |portal_url, errors| %>
  <%= portal_url %>
  <% errors.each do |error| %>
    * <%= error %>
  <% end %>

<% end %>
CMS link: <%= "#{root_url}#{rails_admin.edit_path(locale: :en, model_name: 'gallery', id: @gallery.id).sub('/','')}" %>
